import SwiftUI

struct MyActivityInfo: View {
    // var activity: Activity
    var body: some View {
        Text("Activity Info")
    }
}

struct MyActivityInfo_Previews: PreviewProvider {
    static var previews: some View {
        MyActivityInfo( /* activity: activityData[0] */ )
    }
}
